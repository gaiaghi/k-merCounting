object Main {

  def main(args: Array[String]): Unit = {
    /*
    * args(0) = path of the fasta file
    * */

    //read arguments
    val fileName = if (args.length > 0) args(0) else "data/humantest.fna"

    //loading the fasta file

  }
}